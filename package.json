{
  "name": "everlon",
  "version": "0.0.0",
  "description": "An infinite, unstoppable World",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "mud": "node node_modules/@latticexyz/cli/dist/mud.js",
    "build": "yarn run clean && yarn workspaces foreach -A run build",
    "clean": "concurrently \"yarn workspace @tenetxyz/registry run clean\" \"yarn workspace @tenetxyz/base-ca run clean\" \"yarn workspace @tenetxyz/base-world run clean\"",
    "dev": "yarn run clean && yarn run dev-no-clean",
    "registry": "concurrently -n registry -c red \"yarn workspace @tenetxyz/registry run build && yarn workspace @tenetxyz/registry run deploy\"",
    "base:ca": "concurrently -n base-ca -c magenta \"yarn workspace @tenetxyz/base-ca run initialize\"",
    "base:world": "concurrently -n base-world -c cyan \"yarn workspace @tenetxyz/base-world run initialize\"",
    "dev-no-clean": "concurrently -n framework -c white \"yarn run registry && yarn run base:ca && yarn run base:world\"",
    "foundry:up": "curl -L https://foundry.paradigm.xyz | bash && bash $HOME/.foundry/bin/foundryup",
    "initialize": "yarn workspaces run initialize",
    "mud:up": "yarn workspaces foreach -t run yarn mud set-version -v canary && yarn install",
    "prepare": "(forge --version || yarn foundry:up)",
    "test": "yarn workspaces run test",
    "reinstall-deps": "sh scripts/reinstall_dependencies.sh",
    "rollback-to-snapshot": "sh scripts/rollback/rollback_to_snapshot.sh"
  },
  "devDependencies": {
    "@latticexyz/cli": "link:../mud/packages/cli",
    "@solidstate/contracts": "^0.0.57",
    "@typescript-eslint/eslint-plugin": "5.46.1",
    "@typescript-eslint/parser": "5.46.1",
    "concurrently": "^8.0.1",
    "eslint": "8.29.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.5"
  },
  "engines": {
    "node": "18.x",
    "yarn": ">=3.6.0"
  },
  "packageManager": "yarn@3.6.4",
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.4.0",
    "@fortawesome/free-brands-svg-icons": "^6.4.0",
    "@fortawesome/react-fontawesome": "^0.2.0",
    "web3": "^4.2.2"
  }
}
