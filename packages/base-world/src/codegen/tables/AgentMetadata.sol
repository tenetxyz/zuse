// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0;

/* Autogenerated file. Do not edit manually. */

// Import schema type
import { SchemaType } from "@latticexyz/schema-type/src/solidity/SchemaType.sol";

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { Schema, SchemaLib } from "@latticexyz/store/src/Schema.sol";
import { PackedCounter, PackedCounterLib } from "@latticexyz/store/src/PackedCounter.sol";

bytes32 constant _tableId = bytes32(abi.encodePacked(bytes16(""), bytes16("AgentMetadata")));
bytes32 constant AgentMetadataTableId = _tableId;

struct AgentMetadataData {
  uint256 lastUpdateBlock;
  uint32 numMoves;
}

library AgentMetadata {
  /** Get the table's key schema */
  function getKeySchema() internal pure returns (Schema) {
    SchemaType[] memory _schema = new SchemaType[](1);
    _schema[0] = SchemaType.BYTES32;

    return SchemaLib.encode(_schema);
  }

  /** Get the table's value schema */
  function getValueSchema() internal pure returns (Schema) {
    SchemaType[] memory _schema = new SchemaType[](2);
    _schema[0] = SchemaType.UINT256;
    _schema[1] = SchemaType.UINT32;

    return SchemaLib.encode(_schema);
  }

  /** Get the table's key names */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](1);
    keyNames[0] = "agentObjectEntityId";
  }

  /** Get the table's field names */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](2);
    fieldNames[0] = "lastUpdateBlock";
    fieldNames[1] = "numMoves";
  }

  /** Register the table's key schema, value schema, key names and value names */
  function register() internal {
    StoreSwitch.registerTable(_tableId, getKeySchema(), getValueSchema(), getKeyNames(), getFieldNames());
  }

  /** Register the table's key schema, value schema, key names and value names (using the specified store) */
  function register(IStore _store) internal {
    _store.registerTable(_tableId, getKeySchema(), getValueSchema(), getKeyNames(), getFieldNames());
  }

  /** Get lastUpdateBlock */
  function getLastUpdateBlock(bytes32 agentObjectEntityId) internal view returns (uint256 lastUpdateBlock) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 0, getValueSchema());
    return (uint256(Bytes.slice32(_blob, 0)));
  }

  /** Get lastUpdateBlock (using the specified store) */
  function getLastUpdateBlock(
    IStore _store,
    bytes32 agentObjectEntityId
  ) internal view returns (uint256 lastUpdateBlock) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 0, getValueSchema());
    return (uint256(Bytes.slice32(_blob, 0)));
  }

  /** Set lastUpdateBlock */
  function setLastUpdateBlock(bytes32 agentObjectEntityId, uint256 lastUpdateBlock) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    StoreSwitch.setField(_tableId, _keyTuple, 0, abi.encodePacked((lastUpdateBlock)), getValueSchema());
  }

  /** Set lastUpdateBlock (using the specified store) */
  function setLastUpdateBlock(IStore _store, bytes32 agentObjectEntityId, uint256 lastUpdateBlock) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    _store.setField(_tableId, _keyTuple, 0, abi.encodePacked((lastUpdateBlock)), getValueSchema());
  }

  /** Get numMoves */
  function getNumMoves(bytes32 agentObjectEntityId) internal view returns (uint32 numMoves) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 1, getValueSchema());
    return (uint32(Bytes.slice4(_blob, 0)));
  }

  /** Get numMoves (using the specified store) */
  function getNumMoves(IStore _store, bytes32 agentObjectEntityId) internal view returns (uint32 numMoves) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 1, getValueSchema());
    return (uint32(Bytes.slice4(_blob, 0)));
  }

  /** Set numMoves */
  function setNumMoves(bytes32 agentObjectEntityId, uint32 numMoves) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    StoreSwitch.setField(_tableId, _keyTuple, 1, abi.encodePacked((numMoves)), getValueSchema());
  }

  /** Set numMoves (using the specified store) */
  function setNumMoves(IStore _store, bytes32 agentObjectEntityId, uint32 numMoves) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    _store.setField(_tableId, _keyTuple, 1, abi.encodePacked((numMoves)), getValueSchema());
  }

  /** Get the full data */
  function get(bytes32 agentObjectEntityId) internal view returns (AgentMetadataData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    bytes memory _blob = StoreSwitch.getRecord(_tableId, _keyTuple, getValueSchema());
    return decode(_blob);
  }

  /** Get the full data (using the specified store) */
  function get(IStore _store, bytes32 agentObjectEntityId) internal view returns (AgentMetadataData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    bytes memory _blob = _store.getRecord(_tableId, _keyTuple, getValueSchema());
    return decode(_blob);
  }

  /** Set the full data using individual values */
  function set(bytes32 agentObjectEntityId, uint256 lastUpdateBlock, uint32 numMoves) internal {
    bytes memory _data = encode(lastUpdateBlock, numMoves);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    StoreSwitch.setRecord(_tableId, _keyTuple, _data, getValueSchema());
  }

  /** Set the full data using individual values (using the specified store) */
  function set(IStore _store, bytes32 agentObjectEntityId, uint256 lastUpdateBlock, uint32 numMoves) internal {
    bytes memory _data = encode(lastUpdateBlock, numMoves);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    _store.setRecord(_tableId, _keyTuple, _data, getValueSchema());
  }

  /** Set the full data using the data struct */
  function set(bytes32 agentObjectEntityId, AgentMetadataData memory _table) internal {
    set(agentObjectEntityId, _table.lastUpdateBlock, _table.numMoves);
  }

  /** Set the full data using the data struct (using the specified store) */
  function set(IStore _store, bytes32 agentObjectEntityId, AgentMetadataData memory _table) internal {
    set(_store, agentObjectEntityId, _table.lastUpdateBlock, _table.numMoves);
  }

  /** Decode the tightly packed blob using this table's schema */
  function decode(bytes memory _blob) internal pure returns (AgentMetadataData memory _table) {
    _table.lastUpdateBlock = (uint256(Bytes.slice32(_blob, 0)));

    _table.numMoves = (uint32(Bytes.slice4(_blob, 32)));
  }

  /** Tightly pack full data using this table's schema */
  function encode(uint256 lastUpdateBlock, uint32 numMoves) internal pure returns (bytes memory) {
    return abi.encodePacked(lastUpdateBlock, numMoves);
  }

  /** Encode keys as a bytes32 array using this table's schema */
  function encodeKeyTuple(bytes32 agentObjectEntityId) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    return _keyTuple;
  }

  /* Delete all data for given keys */
  function deleteRecord(bytes32 agentObjectEntityId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    StoreSwitch.deleteRecord(_tableId, _keyTuple, getValueSchema());
  }

  /* Delete all data for given keys (using the specified store) */
  function deleteRecord(IStore _store, bytes32 agentObjectEntityId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = agentObjectEntityId;

    _store.deleteRecord(_tableId, _keyTuple, getValueSchema());
  }
}
